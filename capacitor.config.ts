import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sentinel.security',
  appName: 'Sentinel Security AI',
  webDir: 'dist'
};

export default config;
